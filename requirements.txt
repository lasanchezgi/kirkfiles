# The Kirk Files — dependencias
#
# Nota: podcasts.happyscribe.com está detrás de Cloudflare (managed challenge).
# requests + BeautifulSoup planos reciben el reto JS, no el HTML. Usamos
# curl_cffi con impersonation TLS de Chrome para atravesarlo.

# Scraping
curl_cffi>=0.7          # cliente HTTP con impersonation TLS (bypass Cloudflare)
beautifulsoup4>=4.12
lxml>=5.0

# LLM / embeddings (Fases A.5–D)
openai>=1.40

# Base de datos
sqlite-vec>=0.1.3

# UI (Fase E)
streamlit>=1.36

# CLI / config
typer>=0.12
python-dotenv>=1.0
pyyaml>=6.0

# Testing
pytest>=8.0
